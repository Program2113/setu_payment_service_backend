# Test dependencies — install with:
#   pip install -r requirements_test.txt
pytest
pytest-asyncio
anyio
httpx
aiosqlite
# App dependencies (also required for tests)
fastapi
uvicorn
sqlalchemy
asyncpg
pydantic